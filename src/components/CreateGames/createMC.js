import React from "react";
import { Title } from '../../styles/PageElements.js';

const CreateMC = () => {
   return (
      <div>
         <Title>Create a Multiple Choice Quiz</Title>
      </div>
   );
};

export default CreateMC;