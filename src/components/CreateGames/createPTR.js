import React from "react";
import { Title } from '../../styles/PageElements.js';

const CreatePTR = () => {
   return (
      <div>
         <Title>Create a Prompt / Text Response Game</Title>
      </div>
   );
};

export default CreatePTR;